<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F1F8E9">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp"
        android:gravity="center_horizontal">

        <!-- Botón de fecha -->
        <Button
            android:id="@+id/btnElegirFecha"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="📅 Elegir fecha"
            android:textStyle="bold"
            android:backgroundTint="#81C784"
            android:textColor="#FFFFFF"
            android:elevation="4dp"
            android:padding="12dp"
            android:layout_marginBottom="16dp" />

        <!-- Fecha seleccionada -->
        <TextView
            android:id="@+id/tvFechaElegida"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="🗓️ Ninguna fecha seleccionada"
            android:textColor="#388E3C"
            android:textSize="18sp"
            android:textStyle="bold"
            android:gravity="center"
            android:layout_marginBottom="16dp" />

        <!-- Contenedor de nota -->
        <TextView
            android:text="Nota del día"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="#555555"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="6dp" />

        <EditText
            android:id="@+id/etTextoDiario"
            android:layout_width="match_parent"
            android:layout_height="200dp"
            android:hint="Aquí aparecerá el texto..."
            android:enabled="false"
            android:background="@drawable/bg_diario_caja"
            android:padding="16dp"
            android:gravity="top"
            android:textColor="#212121"
            android:textSize="16sp"
            android:lineSpacingExtra="4dp"
            android:textIsSelectable="true" />
    </LinearLayout>
</ScrollView>